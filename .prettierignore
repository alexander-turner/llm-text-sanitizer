# Dependencies
node_modules/

# Build outputs
dist/
build/
types/

# Package manager
pnpm-lock.yaml

# Python
__pycache__/
*.pyc
.venv/
.uv/
uv.lock

# Generated: the self-contained CLI bundled into the Python wheel at build time.
python/agent_input_sanitizer/_bundled/

# Template sync
.template-version

# Coverage
coverage/

# Stryker mutation testing (generated)
.stryker-tmp/
reports/mutation/

# Release sections are written by scripts/promote-changelog.mjs — keep the exact
# ASCII `## [x.y.z] - date` formatting; don't let a formatter rewrite it.
CHANGELOG.md

# Generated: the invisible-charset SSOT is written by scripts/gen-invisible-charset.mjs
# with JSON.stringify (one code point per line); prettier would repack the array
# and fight the generator on every regeneration. The generator owns its format.
python/agent_input_sanitizer/data/invisible-charset.json
